📚 Документация проекта: Эмулятор Raspberry Pi для Web
🎯 Обзор проекта
Веб-эмулятор Raspberry Pi - это интерактивное веб-приложение, которое позволяет выполнять код Python для Raspberry Pi в изолированной среде с визуализацией работы GPIO пинов и датчиков в реальном времени.

🌟 Основные возможности
Выполнение Python кода для Raspberry Pi в безопасной среде

Визуализация состояния GPIO пинов в реальном времени

WebSocket соединение для мгновенных обновлений

Эмуляция датчиков и периферийных устройств

Интуитивный веб-интерфейс

📁 Структура проекта
text
raspberry-emulator-web/
├── 📁 backend/                    # Серверная часть (FastAPI)
│   ├── 🐳 Dockerfile             # Docker-образ для бэкенда
│   ├── 🐳 main.py                # Основной файл FastAPI
│   ├── 🐳 websocket_manager.py   # Управление WebSocket соединениями
│   ├── 🐳 docker_manager.py      # Управление Docker контейнерами
│   ├── 🐳 gpio_emulator.py       # Эмуляция GPIO и датчиков
│   └── 🐳 requirements.txt       # Зависимости Python
├── 📁 frontend/                  # Клиентская часть
│   ├── 📄 index.html            # Основная HTML страница
│   ├── 📄 style.css             # Стилизация
│   ├── 📄 script.js             # Основная JavaScript логика
│   ├── 📄 components.js         # Визуальные компоненты
│   └── 📄 websocket-client.js   # WebSocket клиент
├── 📁 emulation-docker/         # Docker для эмуляции RPi
│   ├── 🐳 Dockerfile            # Образ с эмуляцией Raspberry Pi
│   ├── 🐳 custom_gpio.py        # Кастомная реализация GPIO
│   ├── 🐳 custom_components.py  # Эмуляция компонентов
│   ├── 🐳 runner.py             # Скрипт выполнения пользовательского кода
│   └── 🐳 test_script.py        # Тестовый скрипт
├── 🐳 docker-compose.yml        # Конфигурация Docker Compose
├── 🐳 .gitignore
└── 📄 README.md                 # Эта документация
🚀 Быстрый старт
Предварительные требования
Docker (версия 20.10.0+)

Docker Compose (версия 1.29.0+)

Python 3.9+ (для разработки)

Установка и запуск
Клонируйте и подготовьте проект:

bash
git clone <repository-url>
cd raspberry-emulator-web
Соберите и запустите контейнеры:

bash
docker-compose up -d --build
Откройте приложение:

text
http://localhost:8000
Остановка приложения:

bash
docker-compose down
🛠️ Техническая архитектура
Backend (FastAPI)
FastAPI - современный веб-фреймворк

WebSocket - реальное время обновление интерфейса

Docker SDK - управление контейнерами выполнения кода

Uvicorn - ASGI сервер

Frontend (JavaScript/HTML/CSS)
Vanilla JavaScript - без фреймворков для простоты

WebSocket API - коммуникация с сервером

CSS Grid/Flexbox - адаптивная верстка

ES6+ - современный JavaScript

Эмуляция
Кастомная реализация RPi.GPIO - эмуляция GPIO пинов

Docker изоляция - безопасное выполнение кода

Реальный time execution - постепенное выполнение с задержками

🔧 Конфигурация
Переменные окружения
bash
DOCKER_HOST=unix:///var/run/docker.sock  # Docker socket
PYTHONUNBUFFERED=1                       # Небуферизированный вывод
Порт по умолчанию
Backend API: 8000

WebSocket: ws://localhost:8000/ws/execute

💻 Использование
1. Написание кода
Введите код Python в редакторе:

python
import RPi.GPIO as GPIO
import time

GPIO.setmode(GPIO.BCM)
GPIO.setup(18, GPIO.OUT)

try:
    for i in range(10):
        GPIO.output(18, GPIO.HIGH)
        time.sleep(0.5)
        GPIO.output(18, GPIO.LOW)
        time.sleep(0.5)
finally:
    GPIO.cleanup()
2. Выполнение кода
Нажмите "▶️ Выполнить" для запуска

Наблюдайте за выполнением в реальном времени

Следите за состоянием GPIO пинов в визуализаторе

3. Управление выполнением
⏹️ Остановить - прервать выполнение

🧹 Очистить - очистить консоль вывода

🎨 Визуализация
GPIO пины
🔴 Красный - неактивное состояние

🟢 Зеленый - активное состояние (HIGH)

Анимация - плавные переходы состояний

Датчики
Температура - текущие значения в °C

Влажность - показания в %

Дистанция - расстояние в cm

⚙️ API Endpoints
WebSocket (/ws/execute)
javascript
// Отправка кода на выполнение
{
  "type": "execute",
  "code": "python code here"
}

// Остановка выполнения
{
  "type": "stop"
}
Ответы WebSocket
javascript
// Вывод выполнения
{"type": "output", "content": "message"}

// Ошибка
{"type": "error", "content": "error message"}

// Начало выполнения
{"type": "execution_started", "message": "started"}

// Завершение выполнения
{"type": "execution_completed", "message": "completed"}

🐛 Отладка и диагностика
Просмотр логов
bash
# Логи бэкенда
docker-compose logs backend

# Логи в реальном времени
docker-compose logs -f backend

# Проверка контейнеров
docker-compose ps
Тестирование соединения
bash
# Проверка WebSocket
curl -i -N -H "Connection: Upgrade" -H "Upgrade: websocket" http://localhost:8000/ws/execute

# Проверка статических файлов
curl http://localhost:8000/static/style.css
🚀 Развертывание в production
1. Настройка Nginx
nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        
        # WebSocket support
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
2. Настройка SSL
bash
# Использование Let's Encrypt
certbot --nginx -d your-domain.com